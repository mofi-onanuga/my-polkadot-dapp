import { map } from "rxjs/operators";
export function getBlock(client, options) {
    switch (options?.tag) {
        case "best":
            return client.bestBlocks$.pipe(map((blockInfos) => blockInfos.at(0)));
        case "finalized":
        default:
            return client.finalizedBlock$;
    }
}
//# sourceMappingURL=get-block.js.map