import { BigIntMath } from "@reactive-dot/utils";
export function spendableBalance({ free, reserved, frozen, existentialDeposit, includesExistentialDeposit = false, }) {
    return BigIntMath.max(0n, free -
        BigIntMath.max(frozen - reserved, includesExistentialDeposit ? 0n : existentialDeposit));
}
//# sourceMappingURL=spendable-balance.js.map