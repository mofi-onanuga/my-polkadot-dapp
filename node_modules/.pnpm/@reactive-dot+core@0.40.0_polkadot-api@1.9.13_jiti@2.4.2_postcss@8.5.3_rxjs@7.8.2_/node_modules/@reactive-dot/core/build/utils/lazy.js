const empty = Symbol("empty");
export function lazy(get) {
    let value = empty;
    return () => {
        if (value !== empty) {
            return value;
        }
        value = get();
        return value;
    };
}
//# sourceMappingURL=lazy.js.map