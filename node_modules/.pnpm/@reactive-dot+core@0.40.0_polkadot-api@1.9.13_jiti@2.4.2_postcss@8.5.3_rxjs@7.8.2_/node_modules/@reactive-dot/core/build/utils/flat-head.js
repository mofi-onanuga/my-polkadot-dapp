export function flatHead(value) {
    if (Array.isArray(value) && value.length === 1) {
        return value.at(0);
    }
    // @ts-expect-error TODO: fix this
    return value;
}
//# sourceMappingURL=flat-head.js.map