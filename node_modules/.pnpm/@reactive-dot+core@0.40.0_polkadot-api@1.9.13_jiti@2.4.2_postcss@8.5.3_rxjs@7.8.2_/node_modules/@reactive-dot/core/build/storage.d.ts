import type { SimpleStorage } from "./simple-storage.js";
export type StorageOptions = {
    prefix: string;
    storage: SimpleStorage;
};
export declare class Storage<TKey extends string = string> implements SimpleStorage {
    #private;
    readonly prefix: string;
    constructor(options: StorageOptions);
    getItem(key: TKey): string | null;
    removeItem(key: TKey): void;
    setItem(key: TKey, value: string): void;
    join<TKeyOverride extends string | void = void>(path: string): Storage<TKeyOverride extends void ? TKey : TKeyOverride>;
}
export declare const defaultStorage: Storage<string>;
//# sourceMappingURL=storage.d.ts.map