export function findAllIndexes(array, predicate) {
    return array.reduce((indexes, item, index) => {
        if (predicate(item)) {
            indexes.push(index);
        }
        return indexes;
    }, []);
}
//# sourceMappingURL=find-all-indexes.js.map