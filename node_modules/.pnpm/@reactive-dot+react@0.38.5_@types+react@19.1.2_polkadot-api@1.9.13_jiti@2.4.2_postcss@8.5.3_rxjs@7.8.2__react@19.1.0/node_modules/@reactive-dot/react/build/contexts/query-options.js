import { jsx as _jsx } from "react/jsx-runtime";
import { createContext, useMemo } from "react";
export const SubscriptionContext = createContext({ active: true });
/**
 * React context to control subscription options.
 *
 * @param props - Component props
 * @returns React element
 */
export function QueryOptionsProvider({ active, children }) {
    return (_jsx(SubscriptionContext, { value: useMemo(() => ({ active }), [active]), children: children }));
}
//# sourceMappingURL=query-options.js.map