export function nativeTokenInfoFromChainSpecData(chainSpecData) {
    const symbol = chainSpecData.properties.tokenSymbol;
    const decimals = chainSpecData.properties.tokenDecimals;
    return {
        code: typeof symbol === "string"
            ? symbol
            : Array.isArray(symbol) && typeof symbol[0] === "string"
                ? symbol[0]
                : undefined,
        decimals: typeof decimals === "number"
            ? decimals
            : Array.isArray(decimals) && typeof decimals[0] === "number"
                ? decimals[0]
                : undefined,
    };
}
//# sourceMappingURL=native-token-info-from-chain-spec-data.js.map